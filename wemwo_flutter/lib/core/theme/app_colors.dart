import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Pagrindinė spalvų schema – kai norėsim, pakeisim tik čia
  static const primary = Color(0xFF1D3557);
  static const primaryLight = Color(0xFF457B9D);
  static const accent = Color(0xFFE63946);

  static const background = Color(0xFFF7F7F9);
  static const surface = Colors.white;

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6C6C6C);

  static const success = Color(0xFF2A9D8F);
  static const warning = Color(0xFFF4A261);
  static const error = Color(0xFFE76F51);
}
